import { Box } from '@mui/material';
import DataCard from '../components/DataCard';

const DataList = () => {
  return (
    <Box sx={{ ml: '20%' }}>
      <DataCard />
    </Box>
  );
};

export default DataList;
